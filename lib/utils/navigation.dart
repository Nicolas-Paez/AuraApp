import 'package:flutter/material.dart';

// Shared navigator key used across the app (MaterialApp and notification handlers)
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
